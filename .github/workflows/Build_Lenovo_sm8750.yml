name: Build_Lenovo_sm8750
permissions:
  contents: read
  actions: write
on:
  workflow_dispatch:
    inputs:
      ksu_type:
        description: "📝Select the KSU to integrate:"
        required: true
        type: choice
        options:
          - 'KernelSU'
          - 'KernelSU Next'
          - 'SukiSU'
          - 'ReSukiSU'
        default: 'KernelSU'

      kernel_version:
        description: "📝Select the version to build:"
        required: true
        type: choice
        options:
          - '6.6.56'
          - '6.6.89'
          - '6.6.102'
          - '6.6.118'
        default: '6.6.89'

      custom_kernel_suffix:
        description: "✏️ Custom kernel name (leave blank to use the stock kernel name)"
        required: false
        default: ''

      custom_kernel_time:
        description: "⏰ Custom build time (leave blank to use the stock kernel time)"
        required: false
        default: ''

      enable_bbg:
        description: "Enable BBG baseband guard"
        required: false
        default: false
        type: boolean

      enable_ReKernel:
        description: "Enable Re-Kernel support"
        required: false
        default: false
        type: boolean

      enable_Adios:
        description: "Enable ADIOS I/O scheduler support"
        required: true
        default: true
        type: boolean

      enable_kpm:
        description: "Enable KPM"
        required: false
        default: true
        type: boolean

      enable_susfs:
        description: "Enable SUSFS"
        required: true
        default: true
        type: boolean

      enable_signed:
        description: "Sign boot with AOSP keys"
        required: false
        default: true
        type: boolean



jobs:
  build:
    name: Build_${{ github.event.inputs.kernel_version }}
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
      KSU_TYPE: ${{ github.event.inputs.ksu_type }}
      KERNEL_VERSION: ${{ github.event.inputs.kernel_version }}
      CUSTOM_KERNEL_SUFFIX: ${{ github.event.inputs.custom_kernel_suffix }}
      CUSTOM_KERNEL_TIME: ${{ github.event.inputs.custom_kernel_time }}
      STOCK_BUILD_USER: kleaf
      STOCK_BUILD_HOST: build-host
      STOCK_COMPILER_INFO: Android (11368308, +pgo, +bolt, +lto, +mlgo, based on r510928) clang version 18.0.0
      TOOLCHAIN_CACHE_ROOT: .toolchain-cache
      ENABLE_REKERNEL: ${{ github.event.inputs.enable_ReKernel }}
      ENABLE_ADIOS: ${{ github.event.inputs.enable_Adios }}
      ENABLE_BBG: ${{ github.event.inputs.enable_bbg }}
      ENABLE_KPM: ${{ github.event.inputs.enable_kpm }}
      ENABLE_SUSFS: ${{ github.event.inputs.enable_susfs }}
      ENABLE_SIGNED: ${{ github.event.inputs.enable_signed }}
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: ⚙️ Set Kernel version config
        run: |
          echo "KERNEL_TIME=${{ env.CUSTOM_KERNEL_TIME }}" >> $GITHUB_ENV
          case "${{ env.KERNEL_VERSION }}" in
            "6.6.56")
              echo "os_patch_level=2024-11" >> $GITHUB_ENV
              echo "releases=TB322FC_1.1.11.120" >> $GITHUB_ENV
              ;;
            "6.6.89")
              echo "os_patch_level=2025-06" >> $GITHUB_ENV
              echo "releases=ColorOS_16_400_OPD_Boot" >> $GITHUB_ENV
              ;;
            "6.6.102")
              echo "os_patch_level=2025-10" >> $GITHUB_ENV
              echo "releases=TB322FC_1.5.10.096" >> $GITHUB_ENV
              ;;
            "6.6.118")
              echo "os_patch_level=2026-01" >> $GITHUB_ENV
              echo "releases=TB322FC_1.5.10.183" >> $GITHUB_ENV
              ;;
          esac
           if [ -z "${{ env.CUSTOM_KERNEL_SUFFIX }}" ]; then
            case "${{ env.KERNEL_VERSION }}" in
              "6.6.56")
                DEFAULT_SUFFIX='-android15-8-g38447e018c92-ab12829524-4k'
                ;;
              "6.6.89")
                DEFAULT_SUFFIX='-android15-8-g14220ae4ce65-ab13680582-4k'
                ;;
              "6.6.102")
                DEFAULT_SUFFIX='-android15-8-g06bc416365ef-ab14559830-4k'
                ;;
              "6.6.118")
                DEFAULT_SUFFIX='-android15-8-g350f378eb496-ab14943902-4k'
                ;;
            esac
            echo "DEFAULT_SUFFIX=$DEFAULT_SUFFIX" >> $GITHUB_ENV
            echo "Using stock kernel name: $DEFAULT_SUFFIX"
          else
            echo "Using custom name: ${{ env.CUSTOM_KERNEL_SUFFIX }}"
            echo "DEFAULT_SUFFIX=${{ env.CUSTOM_KERNEL_SUFFIX }}" >> $GITHUB_ENV
          fi
          if [ -z "${{ env.CUSTOM_KERNEL_TIME }}" ]; then
            case "${{ env.KERNEL_VERSION }}" in
              6.6.56)
                KERNEL_TIME='Thu Dec 19 17:58:46 UTC 2024'
                ;;
              6.6.89)
                KERNEL_TIME='Mon Jun 23 07:30:57 UTC 2025'
                ;;
              6.6.102)
                KERNEL_TIME='Tue Dec 9 15:43:22 UTC 2025'
                ;;
              6.6.118)
                KERNEL_TIME='Thu Feb 26 02:40:50 UTC 2026'
                ;;
            esac
            echo "KERNEL_TIME=$KERNEL_TIME" >> $GITHUB_ENV
            echo "Using stock kernel time: $KERNEL_TIME"
          else
            echo "Using custom build time: ${{ env.CUSTOM_KERNEL_TIME }}"
            echo "KERNEL_TIME=${{ env.CUSTOM_KERNEL_TIME }}" >> $GITHUB_ENV
          fi

      - name: 📥 Restore toolchain cache
        id: toolchain-cache
        uses: actions/cache/restore@v5
        with:
          path: |
            ${{ env.TOOLCHAIN_CACHE_ROOT }}/clang18
            ${{ env.TOOLCHAIN_CACHE_ROOT }}/build-tools
          key: toolchain-clang-r510928-build-tools-r510928-v1

      - name: 🛠 Install dependencies, initialize repo, and sync
        run: |
          rm -rf kernel_workspace
          mkdir kernel_workspace
          mkdir -p "${{ env.TOOLCHAIN_CACHE_ROOT }}"
          cd kernel_workspace

          sudo apt-mark hold firefox &&
          sudo apt-mark hold libc-bin &&
          sudo apt purge man-db &&
          sudo rm -rf /var/lib/man-db/auto-update &&
          sudo apt update &&
          sudo apt-get install -y --no-install-recommends binutils python-is-python3 libssl-dev libelf-dev ccache p7zip-full zstd &
          echo "Cloning source repository..."
          COMMON_ARCHIVE_URL="https://github.com/showdo/Build_Lenovo_sm8750/releases/download/common/${{ env.os_patch_level }}.tar.gz"
          if [ "${{ env.KERNEL_VERSION }}" = "6.6.118" ]; then
            COMMON_ARCHIVE_URL="https://android.googlesource.com/kernel/common/+archive/refs/tags/android15-6.6-2026-01_r1.tar.gz"
          fi
          echo "Source archive: $COMMON_ARCHIVE_URL"
          aria2c -s16 -x16 -k1M "$COMMON_ARCHIVE_URL" -o common.tar.gz && \
          mkdir -p common && \
          tar -xzf common.tar.gz -C common && \
          rm -f common.tar.gz &


          if [ -d "../${{ env.TOOLCHAIN_CACHE_ROOT }}/clang18/bin" ]; then
            echo "Restoring cached llvm-clang18 toolchain..." &&
            cp -a "../${{ env.TOOLCHAIN_CACHE_ROOT }}/clang18" ./clang18 &
          else
            echo "Cloning llvm-clang18 toolchain..." &&
            mkdir -p clang18 &&
            aria2c -s16 -x16 -k1M https://github.com/cctv18/oneplus_sm8650_toolchain/releases/download/LLVM-Clang18-r510928/clang-r510928.zip -o clang.zip &&
            unzip -q clang.zip -d clang18 &&
            rm -rf clang.zip &
          fi

          if [ -d "../${{ env.TOOLCHAIN_CACHE_ROOT }}/build-tools/bin" ]; then
            echo "Restoring cached build tools..." &&
            cp -a "../${{ env.TOOLCHAIN_CACHE_ROOT }}/build-tools" ./build-tools &
          else
            echo "Cloning build tools..." &&
            aria2c -s16 -x16 -k1M https://github.com/cctv18/oneplus_sm8650_toolchain/releases/download/LLVM-Clang18-r510928/build-tools.zip -o build-tools.zip &&
            unzip -q build-tools.zip &&
            rm -rf build-tools.zip &
          fi

          wait
          if [ ! -d "../${{ env.TOOLCHAIN_CACHE_ROOT }}/clang18/bin" ] && [ -d "clang18/bin" ]; then
            rm -rf "../${{ env.TOOLCHAIN_CACHE_ROOT }}/clang18"
            cp -a clang18 "../${{ env.TOOLCHAIN_CACHE_ROOT }}/clang18"
          fi
          if [ ! -d "../${{ env.TOOLCHAIN_CACHE_ROOT }}/build-tools/bin" ] && [ -d "build-tools/bin" ]; then
            rm -rf "../${{ env.TOOLCHAIN_CACHE_ROOT }}/build-tools"
            cp -a build-tools "../${{ env.TOOLCHAIN_CACHE_ROOT }}/build-tools"
          fi
          echo "All sources and the llvm-Clang18 toolchain have been initialized!"
          echo "Removing ABI protection files and the dirty suffix..."
          rm common/android/abi_gki_protected_exports_* || true
          # if [[ "${{ env.KERNEL_VERSION }}" != "6.6.102" ]]; then
            for f in common/scripts/setlocalversion; do
              sed -i 's/ -dirty//g' "$f"
              sed -i '$i res=$(echo "$res" | sed '\''s/-dirty//g'\'')' "$f"
            done
          # fi

      - name: 📥 Save toolchain cache
        if: ${{ steps.toolchain-cache.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@v5
        with:
          path: |
            ${{ env.TOOLCHAIN_CACHE_ROOT }}/clang18
            ${{ env.TOOLCHAIN_CACHE_ROOT }}/build-tools
          key: toolchain-clang-r510928-build-tools-r510928-v1

      - name: 🛠 Set ccache dir
        run: |
          echo "CCACHE_DIR=$HOME/.ccache_${{ env.KSU_TYPE }}_${{ env.KERNEL_VERSION }}" >> $GITHUB_ENV
          echo "CCACHE_MAXSIZE=10G" >> $GITHUB_ENV
          echo "Current disk space:"
          df -h
          echo "Kernel version being built: ${{ env.KERNEL_VERSION }}"

      - name: 🛠 Set build identity
        working-directory: kernel_workspace/common
        run: |
          set -e
          if [ -f "scripts/mkcompile_h" ]; then
            if grep -q "LINUX_COMPILE_BY=" scripts/mkcompile_h; then
              sed -i "s|LINUX_COMPILE_BY=.*|LINUX_COMPILE_BY=\"${{ env.STOCK_BUILD_USER }}\"|" scripts/mkcompile_h
              sed -i "s|LINUX_COMPILE_HOST=.*|LINUX_COMPILE_HOST=\"${{ env.STOCK_BUILD_HOST }}\"|" scripts/mkcompile_h
            fi
            if grep -q "LINUX_COMPILER=" scripts/mkcompile_h; then
              sed -i "s|LINUX_COMPILER=.*|LINUX_COMPILER=\"${{ env.STOCK_COMPILER_INFO }}\"|" scripts/mkcompile_h
            fi
          fi
          echo "KBUILD_BUILD_USER=${{ env.STOCK_BUILD_USER }}" >> "$GITHUB_ENV"
          echo "KBUILD_BUILD_HOST=${{ env.STOCK_BUILD_HOST }}" >> "$GITHUB_ENV"
          echo "Using build user: ${{ env.STOCK_BUILD_USER }}"
          echo "Using build host: ${{ env.STOCK_BUILD_HOST }}"
          echo "Using compiler info: ${{ env.STOCK_COMPILER_INFO }}"

      - name: 📥 Restore ccache
        id: ccache-restore
        uses: actions/cache/restore@v5
        with:
          path: ${{ env.CCACHE_DIR }}
          key: ccache-${{ env.KSU_TYPE }}-${{ env.KERNEL_VERSION }}-latest
          restore-keys: |
            ccache-${{ env.KSU_TYPE }}-${{ env.KERNEL_VERSION }}-

      - name: 🔎 Find public ccache in releases
        id: public-ccache
        if: ${{ steps.ccache-restore.outputs.cache-matched-key == '' }}
        uses: actions/github-script@v8
        env:
          PUBLIC_CACHE_REPO: ${{ vars.PUBLIC_CACHE_REPO }}
          PUBLIC_CACHE_TAG: ${{ vars.PUBLIC_CACHE_TAG }}
          KERNEL_VERSION: ${{ env.KERNEL_VERSION }}
        with:
          script: |
            const repoName = process.env.PUBLIC_CACHE_REPO || 'showdo/Public_Ccache';
            const releaseTag = process.env.PUBLIC_CACHE_TAG || 'exported-ccaches';
            const [owner, repo] = repoName.split('/');
            const deviceName = process.env.KERNEL_VERSION;
            let selected = null;

            const escape_regex = (value) => value.replace(/[.*+?^${}()|[\\]\\]/g, '\\$&');
            const exactAssetName = `ccache-${deviceName}.tar.zst`;
            const legacyAssetPattern = new RegExp(`^ccache-(.+-)?${escape_regex(deviceName)}(-[0-9]+)?\\.tar\\.zst$`);

            core.info(`No matching ccache found in the current repository. Searching public cache by device: ${deviceName}`);
            core.info(`Cache source repository: ${owner}/${repo}`);
            core.info(`Cache source tag: ${releaseTag}`);
            core.info(`Preferred public cache asset: ${exactAssetName}`);

            let release;
            try {
              const response = await github.rest.repos.getReleaseByTag({
                owner,
                repo,
                tag: releaseTag,
              });
              release = response.data;
            } catch (error) {
              core.info(`Public cache release not found: ${releaseTag}`);
              return;
            }

            for (const asset of release.assets || []) {
              const isExactMatch = asset.name === exactAssetName;
              const isLegacyMatch = legacyAssetPattern.test(asset.name);
              if (isExactMatch || isLegacyMatch) {
                const shouldReplace =
                  !selected ||
                  (isExactMatch && !selected.exact) ||
                  (isExactMatch === selected.exact && new Date(asset.updated_at) > new Date(selected.updated_at));

                if (shouldReplace) {
                  selected = {
                    name: asset.name,
                    updated_at: asset.updated_at,
                    tag: release.tag_name,
                    repo: `${owner}/${repo}`,
                    exact: isExactMatch,
                  };
                }
              }
            }

            if (!selected) {
              core.info('No usable public ccache asset found. Continuing with an empty cache.');
              return;
            }

            core.info(`Found public cache: ${selected.name} (${selected.repo}@${selected.tag})`);
            core.setOutput('asset-name', selected.name);
            core.setOutput('release-tag', selected.tag);
            core.setOutput('source-repo', selected.repo);

      - name: 📦 Restore public ccache from releases
        if: ${{ steps.ccache-restore.outputs.cache-matched-key == '' && steps.public-ccache.outputs.asset-name != '' }}
        env:
          GH_TOKEN: ${{ github.token }}
          PUBLIC_CACHE_REPO: ${{ steps.public-ccache.outputs.source-repo }}
          PUBLIC_CACHE_TAG: ${{ steps.public-ccache.outputs.release-tag }}
          PUBLIC_CACHE_ASSET: ${{ steps.public-ccache.outputs.asset-name }}
        run: |
          set -euo pipefail
          echo "No matching cache found in the current repository. Downloading public cache: $PUBLIC_CACHE_ASSET"
          rm -f "$PUBLIC_CACHE_ASSET"
          rm -rf "$CCACHE_DIR"
          gh release download "$PUBLIC_CACHE_TAG" --repo "$PUBLIC_CACHE_REPO" --pattern "$PUBLIC_CACHE_ASSET" --dir .
          TEMP_EXTRACT_DIR="$(mktemp -d)"
          tar --use-compress-program=unzstd -xf "$PUBLIC_CACHE_ASSET" -C "$TEMP_EXTRACT_DIR"
          EXTRACTED_CCACHE_DIR="$(find "$TEMP_EXTRACT_DIR" -maxdepth 1 -mindepth 1 -type d -name '.ccache_*' | head -n 1)"
          if [ -z "$EXTRACTED_CCACHE_DIR" ]; then
            echo "::error::No .ccache_* directory found after extracting the public cache"
            exit 1
          fi

          mkdir -p "$CCACHE_DIR"
          shopt -s dotglob nullglob
          mv "$EXTRACTED_CCACHE_DIR"/* "$CCACHE_DIR"/
          shopt -u dotglob nullglob
          rm -rf "$TEMP_EXTRACT_DIR"
          test -d "$CCACHE_DIR"
          ls -lah "$CCACHE_DIR" | head -n 20
          echo "PUBLIC_CCACHE_USED=true" >> "$GITHUB_ENV"
          echo "Public cache restore completed: $CCACHE_DIR"

      - name: 📥 Init ccache
        run: |
          # Set ccache environment variables
          export CCACHE_COMPILERCHECK="none"
          export CCACHE_BASEDIR="${{ github.workspace }}"
          export CCACHE_NOHASHDIR="true"
          export CCACHE_HARDLINK="true"
          export CCACHE_DIR="${{ env.CCACHE_DIR }}"
          export CCACHE_MAXSIZE="${{ env.CCACHE_MAXSIZE }}"

          # Ensure the ccache directory exists
          mkdir -p "$CCACHE_DIR"

          # Reconfigure cache size on every run
          echo "Configuring ccache max size: $CCACHE_MAXSIZE"
          ccache -M "$CCACHE_MAXSIZE"
          ccache -o compression=true

          # Show initial cache status
          echo "Initial ccache status:"
          ccache -s

          # Show detailed info when cache restore hits
          if [ -n "${{ steps.ccache-restore.outputs.cache-matched-key }}" ]; then
            echo "ccache hit details: ${{ steps.ccache-restore.outputs.cache-matched-key }}"
            ccache -sv
          elif [ "${PUBLIC_CCACHE_USED:-false}" = 'true' ]; then
            echo "Public ccache restored. Current cache details:"
            ccache -sv
          else
            echo "No repository cache hit and no public cache found. Building with an empty cache."
          fi

      - name: 🔧 Add Re-Kernel support
        if: ${{ env.ENABLE_REKERNEL == 'true' }}
        working-directory: kernel_workspace/common
        run: |
          PATCH_FILE="${GITHUB_WORKSPACE}/.github/workflows/tools/Re-Kernel_Support.patch"
          cp "$PATCH_FILE" ./
          patch -p1 -N --ignore-whitespace -F 3 < Re-Kernel_Support.patch 2>&1 | tee /tmp/rk.log || true
          rm -f Re-Kernel_Support.patch
          echo "CONFIG_REKERNEL=m" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_REKERNEL_NETWORK=y" >> arch/arm64/configs/gki_defconfig
          echo "✅ Re-Kernel config injected"

      - name: 🔧 Add adios_ioscheduler
        working-directory: kernel_workspace/common
        run: |
          echo "Enabling ADIOS I/O scheduler support..."
          PATCH_FILE="${GITHUB_WORKSPACE}/.github/workflows/tools/adios_ioscheduler.patch"
          if [ ! -f "$PATCH_FILE" ]; then
            echo "::error::ADIOS I/O scheduler patch file not found"
            exit 1
          fi
          if [ -f "block/adios.c" ]; then
            echo "✓ ADIOS I/O scheduler files already exist. Skipping patch application."
          else
            cp "$PATCH_FILE" ./adios_ioscheduler.patch
            echo "Applying ADIOS I/O scheduler patch..."
            if patch -p1 --ignore-whitespace -F 3 < adios_ioscheduler.patch 2>&1 | tee /tmp/adios_patch.log; then
              echo "✓ ADIOS I/O scheduler patch applied successfully"
            else
              echo "::warning::Issue encountered while applying the ADIOS patch"
              if grep -q "malformed patch" /tmp/adios_patch.log; then
                patch -p1 --force --ignore-whitespace < adios_ioscheduler.patch || true
              fi
            fi
            rm -f adios_ioscheduler.patch
          fi
          if ! grep -q "CONFIG_MQ_IOSCHED_ADIOS" arch/arm64/configs/gki_defconfig; then
            echo "CONFIG_MQ_IOSCHED_ADIOS=y" >> arch/arm64/configs/gki_defconfig
            echo "CONFIG_MQ_IOSCHED_DEFAULT_ADIOS=y" >> arch/arm64/configs/gki_defconfig
            echo "✓ ADIOS I/O scheduler support enabled"
          fi
          echo "✓ ADIOS I/O scheduler support setup completed"

      - name: ⚙️ Apply lz4 1.10.0 & zstd 1.5.7 patch
        run: |
          echo "Applying lz4 1.10.0 & zstd 1.5.7 patch..."
          cd kernel_workspace
          git clone https://github.com/cctv18/oppo_oplus_realme_sm8750.git
          cp ./oppo_oplus_realme_sm8750/zram_patch/001-lz4.patch ./common/
          cp ./oppo_oplus_realme_sm8750/zram_patch/lz4armv8.S ./common/lib
          cp ./oppo_oplus_realme_sm8750/zram_patch/002-zstd.patch ./common/
          cd ./common
          git apply -p1 < 001-lz4.patch || true
          patch -p1 < 002-zstd.patch || true

      - name: 🛠 Set baseband guard
        if: ${{ env.ENABLE_BBG == 'true' }}
        run: |
          echo "Enabling kernel-level baseband protection support..."
          cd kernel_workspace
          wget -O- https://github.com/vc-teahouse/Baseband-guard/raw/main/setup.sh | bash
          echo "CONFIG_BBG=y" >> ./common/arch/arm64/configs/gki_defconfig
          sed -i '/^config LSM$/,/^help$/{ /^[[:space:]]*default/ { /baseband_guard/! s/lockdown/lockdown,baseband_guard/ } }' ./common/security/Kconfig


      - name: ⚙️ Set ${{ env.KSU_TYPE }}
        run: |
            cd kernel_workspace
            echo "Configuring ${{ env.KSU_TYPE }}..."
            case "${{ env.KSU_TYPE }}" in
              "KernelSU")
                curl -LSs "https://raw.githubusercontent.com/tiann/KernelSU/main/kernel/setup.sh" | bash -s main
                cd ./KernelSU
                GIT_COMMIT_HASH=$(git rev-parse --short=8 HEAD)
                KSU_VERSION=$(git rev-list --count HEAD 2>/dev/null || echo 114514)
                echo "KSUVER=$KSU_VERSION" >> $GITHUB_ENV
                echo "ksuver=$KSU_VERSION" >> $GITHUB_OUTPUT
                echo "KernelSU commit hash: $GIT_COMMIT_HASH"
                ;;
              "KernelSU Next")
                curl -LSs "https://raw.githubusercontent.com/KernelSU-Next/KernelSU-Next/stable/kernel/setup.sh" | bash -s stable
                cd ./KernelSU-Next
                GIT_COMMIT_HASH=$(git rev-parse --short=8 HEAD)
                KSU_VERSION=$(git rev-list --count HEAD 2>/dev/null || echo 114514)
                echo "KSUVER=$KSU_VERSION" >> $GITHUB_ENV
                echo "ksuver=$KSU_VERSION" >> $GITHUB_OUTPUT
                echo "KernelSU-Next commit hash: $GIT_COMMIT_HASH"
                ;;
              "ReSukiSU")
                curl -LSs "https://raw.githubusercontent.com/ReSukiSU/ReSukiSU/refs/heads/main/kernel/setup.sh" | bash -s main
                cd ./KernelSU
                KSU_VERSION=$(expr $(git rev-list --count main) + 30700 2>/dev/null || echo 114514)
                echo "KSUVER=$KSU_VERSION" >> $GITHUB_ENV
                echo "ksuver=$KSU_VERSION" >> $GITHUB_OUTPUT
                ;;
              *)
                curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/refs/heads/main/kernel/setup.sh" | bash -s builtin
                cd ./KernelSU
                GIT_COMMIT_HASH=$(git rev-parse --short=8 HEAD)
                echo "Current commit hash: $GIT_COMMIT_HASH"
                export KSU_VERSION=$KSU_VERSION
                for i in {1..3}; do
                  KSU_API_VERSION=$(curl -s "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/builtin/kernel/Makefile" |
                    grep -m1 "KSU_VERSION_API :=" |
                    awk -F'= ' '{print $2}' |
                    tr -d '[:space:]')
                  [ -n "$KSU_API_VERSION" ] && break || sleep 1
                done
                [ -z "$KSU_API_VERSION" ] && KSU_API_VERSION="4.1.0"
                echo "KSU_API_VERSION=$KSU_API_VERSION" >> $GITHUB_ENV
                VERSION_DEFINITIONS=$'define get_ksu_version_full\nv\\$1-'"$GIT_COMMIT_HASH"$'\nendef\n\nKSU_VERSION_API := '"$KSU_API_VERSION"$'\nKSU_VERSION_FULL := v'"$KSU_API_VERSION"$'-'"$GIT_COMMIT_HASH"

                sed -i '/define get_ksu_version_full/,/endef/d' kernel/Makefile
                sed -i '/KSU_VERSION_API :=/d' kernel/Makefile
                sed -i '/KSU_VERSION_FULL :=/d' kernel/Makefile
                awk -v def="$VERSION_DEFINITIONS" '
                  /REPO_OWNER :=/ {print; print def; inserted=1; next}
                  1
                  END {if (!inserted) print def}
                ' kernel/Makefile > kernel/Makefile.tmp && mv kernel/Makefile.tmp kernel/Makefile
                KSU_VERSION=$(expr $(git rev-list --count main) + 37185 2>/dev/null || echo 114514)
                echo "KSUVER=$KSU_VERSION" >> $GITHUB_ENV
                echo "ksuver=$KSU_VERSION" >> $GITHUB_OUTPUT
                grep -A10 "REPO_OWNER" kernel/Makefile
                grep "KSU_VERSION_FULL" kernel/Makefile
                echo "SukiSU version: v${KSU_API_VERSION}-${GIT_COMMIT_HASH}"
                ;;
            esac

      - name: 🔧 Set up SUSFS
        if: ${{ env.ENABLE_SUSFS == 'true' }}
        run: |
          cd kernel_workspace
          git clone --depth=1 https://github.com/cctv18/susfs4oki.git susfs4ksu -b oki-android15-6.6
          wget https://raw.githubusercontent.com/cctv18/oppo_oplus_realme_sm8750/main/other_patch/69_hide_stuff.patch -O ./common/69_hide_stuff.patch
          cp ./susfs4ksu/kernel_patches/50_add_susfs_in_gki-android15-6.6.patch ./common/
          cp ./susfs4ksu/kernel_patches/fs/* ./common/fs/
          cp ./susfs4ksu/kernel_patches/include/linux/* ./common/include/linux/
          cd ./common
          patch -p1 < 50_add_susfs_in_gki-android15-6.6.patch || true
          patch -p1 -N -F 3 < 69_hide_stuff.patch || true
          case "${{ env.KERNEL_VERSION }}" in
            6.6.56)
              echo "[INFO] Building kernel 6.6.56. Fixing the undeclared identifier 'vma' build error"
              cd ./fs/proc
              sed -i 's/vma = find_vma(mm, start_vaddr);/struct vm_area_struct *vma = find_vma(mm, start_vaddr);/' task_mmu.c
              ;;
          esac

      - name: 🔧 Set gki_defconfig
        run: |
          cd kernel_workspace
          echo "CONFIG_KSU=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KPM=y" >> ./common/arch/arm64/configs/gki_defconfig

          if [ "${{ env.KSU_TYPE }}" == 'ReSukiSU' ]; then
            echo 'CONFIG_KSU_FULL_NAME_FORMAT="%TAG_NAME%-%COMMIT_SHA%-Re"' >> ./common/arch/arm64/configs/gki_defconfig
          fi

          if [ "${{ env.ENABLE_SUSFS }}" == 'true' ];then
            echo "CONFIG_KSU_SUSFS=y" >> ./common/arch/arm64/configs/gki_defconfig
            echo "CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y" >> ./common/arch/arm64/configs/gki_defconfig
            echo "CONFIG_KSU_SUSFS_SUS_PATH=y" >> ./common/arch/arm64/configs/gki_defconfig
            echo "CONFIG_KSU_SUSFS_SUS_MOUNT=y" >> ./common/arch/arm64/configs/gki_defconfig
            echo "CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT=y" >> ./common/arch/arm64/configs/gki_defconfig
            echo "CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT=y" >> ./common/arch/arm64/configs/gki_defconfig
            echo "CONFIG_KSU_SUSFS_SUS_KSTAT=y" >> ./common/arch/arm64/configs/gki_defconfig
            echo "CONFIG_KSU_SUSFS_TRY_UMOUNT=y" >> ./common/arch/arm64/configs/gki_defconfig
            echo "CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT=y" >> ./common/arch/arm64/configs/gki_defconfig
            echo "CONFIG_KSU_SUSFS_SPOOF_UNAME=y" >> ./common/arch/arm64/configs/gki_defconfig
            echo "CONFIG_KSU_SUSFS_ENABLE_LOG=y" >> ./common/arch/arm64/configs/gki_defconfig
            echo "CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y" >> ./common/arch/arm64/configs/gki_defconfig
            echo "CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG=y" >> ./common/arch/arm64/configs/gki_defconfig
            echo "CONFIG_KSU_SUSFS_OPEN_REDIRECT=y" >> ./common/arch/arm64/configs/gki_defconfig
            echo "CONFIG_KSU_SUSFS_SUS_MAP=y" >> ./common/arch/arm64/configs/gki_defconfig
          else
            echo "SUSFS is not enabled. Skipping SUSFS configuration."
          fi

          echo "CONFIG_TMPFS_XATTR=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_TMPFS_POSIX_ACL=y" >> ./common/arch/arm64/configs/gki_defconfig

          echo "CONFIG_CC_OPTIMIZE_FOR_PERFORMANCE=y" >> ./common/arch/arm64/configs/gki_defconfig

          #Remove check_defconfig
          sudo sed -i 's/check_defconfig//' ./common/build.config.gki

      - name: 🔧 Set kernel name
        run: |
          cd kernel_workspace
          echo "Custom suffix: $DEFAULT_SUFFIX"
          ESCAPED_SUFFIX=$(printf '%s\n' "$DEFAULT_SUFFIX" | sed 's:[\/&]:\\&:g')
          sudo sed -i "s/-4k/$ESCAPED_SUFFIX/g" ./common/arch/arm64/configs/gki_defconfig
          sed -i 's/${scm_version}//' ./common/scripts/setlocalversion

      - name: 📥 Start build timer
        run:
          echo "BUILD_START=$(date +%s)" >> $GITHUB_ENV

      - name: 🔨 Build kernel
        run: |
          WORKDIR="$(pwd)"
          export PATH="/usr/lib/ccache:$PATH"
          export PATH="$WORKDIR/kernel_workspace/clang18/bin:$PATH"
          export PATH="$WORKDIR/kernel_workspace/build-tools/bin:$PATH"
          CLANG_DIR="$WORKDIR/kernel_workspace/clang18/bin"
          CLANG_VERSION="$($CLANG_DIR/clang --version | head -n 1)"
          LLD_VERSION="$($CLANG_DIR/ld.lld --version | head -n 1)"
          echo "Compiler info:"
          echo "Clang version: $CLANG_VERSION"
          echo "LLD version: $LLD_VERSION"
          pahole_version=$(pahole --version 2>/dev/null | head -n1); [ -z "$pahole_version" ] && echo "pahole version: not installed" || echo "pahole version: $pahole_version"

          export CCACHE_LOGFILE="${{ github.workspace }}/kernel_workspace/ccache.log"
          export CCACHE_COMPILERCHECK="none"
          export CCACHE_BASEDIR="${{ github.workspace }}"
          export CCACHE_NOHASHDIR="true"
          export CCACHE_HARDLINK="true"
          export CCACHE_DIR="${{ env.CCACHE_DIR }}"
          export CCACHE_MAXSIZE="3G"
          echo "sloppiness = file_stat_matches,include_file_ctime,include_file_mtime,pch_defines,file_macro,time_macros" >> "$CCACHE_DIR/ccache.conf"

          cd kernel_workspace/common
          wget https://github.com/cctv18/oppo_oplus_realme_sm8750/raw/refs/heads/main/lib/libfakestat.so
          wget https://github.com/cctv18/oppo_oplus_realme_sm8750/raw/refs/heads/main/lib/libfaketimeMT.so
          chmod 777 ./*.so
          export FAKESTAT="2025-05-25 12:00:00"
          export FAKETIME="@2025-05-25 13:00:00"
          echo "FAKESTAT=$FAKESTAT" >> $GITHUB_ENV
          echo "FAKETIME=$FAKETIME" >> $GITHUB_ENV
          SO_DIR=$(pwd)
          export PRELOAD_LIBS="$SO_DIR/libfakestat.so $SO_DIR/libfaketimeMT.so"
          export KBUILD_BUILD_USER="${{ env.STOCK_BUILD_USER }}"
          export KBUILD_BUILD_HOST="${{ env.STOCK_BUILD_HOST }}"
          # Create the CC (compiler) wrapper
          echo '#!/bin/bash' > cc-wrapper
          echo 'export LD_PRELOAD="'$PRELOAD_LIBS'"' >> cc-wrapper
          echo 'export FAKESTAT="'$FAKESTAT'"' >> cc-wrapper
          echo 'export FAKETIME="'$FAKETIME'"' >> cc-wrapper
          echo 'ccache clang "$@"' >> cc-wrapper
          # Create the LD (linker) wrapper
          echo '#!/bin/bash' > ld-wrapper
          echo 'export LD_PRELOAD="'$PRELOAD_LIBS'"' >> ld-wrapper
          echo 'export FAKESTAT="'$FAKESTAT'"' >> ld-wrapper
          echo 'export FAKETIME="'$FAKETIME'"' >> ld-wrapper
          echo 'ld.lld "$@"' >> ld-wrapper

          # Verify that time hijacking works correctly
          echo "--- [Wrapper Test] Creating a generic time hijack test script ---"
          echo '#!/bin/bash' > test-wrapper.sh
          echo 'export LD_PRELOAD="'$PRELOAD_LIBS'"' >> test-wrapper.sh
          echo 'export FAKESTAT="'$FAKESTAT'"' >> test-wrapper.sh
          echo 'export FAKETIME="'$FAKETIME'"' >> test-wrapper.sh
          echo 'echo ">>> Wrapper internal environment check complete."' >> test-wrapper.sh
          echo 'exec "$@"' >> test-wrapper.sh # Execute all passed arguments
          chmod +x test-wrapper.sh
          echo "--- [Wrapper Test] Testing the date command ---"
          ./test-wrapper.sh date
          echo "--- [Wrapper Test] Testing the stat command ---"
          ./test-wrapper.sh stat ./Makefile
          echo "--- [Wrapper Test] Test completed ---"
          chmod +x cc-wrapper ld-wrapper
          echo "--- Pre-build environment time: $(LD_PRELOAD=$PRELOAD_LIBS date) ---"
          echo "--- Pre-build environment file timestamp: ---"
          LD_PRELOAD=$PRELOAD_LIBS stat ./Makefile

          export KBUILD_BUILD_TIMESTAMP="${KERNEL_TIME}"

          # Clear unnecessary .NET, Android NDK, Haskell, and CodeQL runtimes while the kernel builds to free space without blocking later steps
          sudo rm -rf /usr/share/dotnet &
          sudo rm -rf /usr/local/lib/android &
          sudo rm -rf /opt/ghc &
          sudo rm -rf /opt/hostedtoolcache/CodeQL &
          make -j$(nproc --all) LLVM=1 ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- CC="ccache clang" HOSTCC="ccache clang" HOSTCXX="ccache clang++" LD="ld.lld" HOSTLD=ld.lld O=out KCFLAGS+=-O2 KCFLAGS+=-Wno-error gki_defconfig &&
          make -j$(nproc --all) LLVM=1 ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- CC="$(pwd)/cc-wrapper" HOSTCC="ccache clang" HOSTCXX="ccache clang++" LD="$(pwd)/ld-wrapper" HOSTLD=ld.lld O=out KCFLAGS+=-O2 KCFLAGS+=-Wno-error Image

          # Secondary time hijack verification after the build
          echo "--- Post-build environment time: $(LD_PRELOAD=$PRELOAD_LIBS date) ---"
          echo "--- Post-build environment file timestamp: ---"
          LD_PRELOAD=$PRELOAD_LIBS stat ./Makefile
          echo "Kernel build completed!"
          echo "ccache status:"
          ccache -s
          echo "Disk space after build:"
          df -h

      - name: 🕒 Calculate build time
        run: |
          END=$(date +%s)
          TIME=$((END - BUILD_START))
          echo "BUILD_TIME=$TIME" >> $GITHUB_ENV
          echo "Build time: $TIME seconds"

      - name: 📥 Save ccache if needed
        if: ${{ env.BUILD_TIME > 480 || steps.ccache-restore.outputs.cache-matched-key == '' }}
        uses: actions/cache/save@v5
        with:
          path: ${{ env.CCACHE_DIR }}
          key: ccache-${{ env.KSU_TYPE }}-${{ env.KERNEL_VERSION }}-${{ github.run_id }}

      - name: 🧹 Clean old ccache
        if: env.BUILD_TIME > 480
        uses: actions/github-script@v8
        env:
          PREFIX: ccache-${{ env.KSU_TYPE }}-${{ env.KERNEL_VERSION }}
          CURRENT_KEY: ccache-${{ env.KSU_TYPE }}-${{ env.KERNEL_VERSION }}-${{ github.run_id }}
        with:
          script: |
            const { owner, repo } = context.repo;
            const prefix = process.env.PREFIX;
            const currentKey = process.env.CURRENT_KEY;
            let page = 1;
            let deleted = 0;

            core.info('Build time exceeded 8 minutes. ccache has been saved.');
            core.info(`Cleaning old caches with matching prefix: ${prefix}`);

            while (true) {
              const response = await github.rest.actions.getActionsCacheList({
                owner,
                repo,
                per_page: 100,
                page,
              });

              const caches = response.data.actions_caches || [];
              if (caches.length === 0) {
                break;
              }

              for (const cache of caches) {
                if (cache.key.startsWith(prefix) && cache.key !== currentKey) {
                  core.info(`Deleting cache: ${cache.key} (ID: ${cache.id})`);
                  await github.rest.actions.deleteActionsCacheById({
                    owner,
                    repo,
                    cache_id: cache.id,
                  });
                  deleted++;
                }
              }

              if (caches.length < 100) {
                break;
              }
              page++;
            }

            core.info(`Old cache cleanup completed. Deleted ${deleted} caches.`);

      - name: 📦 Apply KPM and patch the kernel
        if: ${{ env.ENABLE_KPM == 'true' }}
        run: |
          echo "Applying KPM and patching the kernel..."
          cd kernel_workspace/common/out/arch/arm64/boot
          curl -LO https://github.com/ShirkNeko/SukiSU_KernelPatch_patch/releases/download/0.12.2/patch_linux
          chmod +x patch_linux
          ./patch_linux
          rm -f Image
          mv oImage Image

      - name: 📦 Rename image
        run: |
          cd kernel_workspace/common/out/arch/arm64/boot
          cp ./Image "./kernel"

      - name: 📤 Upload Image
        uses: actions/upload-artifact@v5
        with:
         name: kernel_image
         path: kernel_workspace/common/out/arch/arm64/boot/kernel

      - name: 📦 Sign boot using AOSP test key
        if: ${{ env.ENABLE_SIGNED == 'true' }}
        run: |
          cd kernel_workspace
          aria2c -s16 -x16 -k1M "https://github.com/showdo/AOSP-Testkey-BootSigner/archive/refs/heads/main.tar.gz" -o boot_avb_tool.tar.gz && \
          mkdir -p boot_avb_tool && \
          tar -xzf boot_avb_tool.tar.gz -C boot_avb_tool && \
          rm -f boot_avb_tool.tar.gz && \
          cp ./common/out/arch/arm64/boot/Image ./boot_avb_tool/AOSP-Testkey-BootSigner-main/kernel
          cd boot_avb_tool/AOSP-Testkey-BootSigner-main && \
          curl -L -o boot.img https://github.com/showdo/AOSP-Testkey-BootSigner/releases/download/${{ env.releases }}/boot.img
          chmod +x ./tools/magiskboot
          ./tools/magiskboot repack boot.img
          rm -f ./boot.img kernel
          mv ./new-boot.img ./boot.img
          python3 rebuild_avb.py --chained-mode

      - name: 📤 Upload signed boot
        if: ${{ env.ENABLE_SIGNED == 'true' }}
        uses: actions/upload-artifact@v5
        with:
         name: Boot_${{ env.KSUVER }}_${{ env.KERNEL_VERSION }}_${{ env.KSU_TYPE }}_AOSP_Signed
         path: kernel_workspace/boot_avb_tool/AOSP-Testkey-BootSigner-main/boot.img

      - name: 🔎 Find Re-Kernel module zip
        id: download-rekernel
        if: ${{ env.ENABLE_REKERNEL == 'true' }}
        uses: actions/github-script@v8
        env:
          KERNEL_MAJOR: "6.6"
        with:
          script: |
            const owner = 'Sakion-Team';
            const repo = 'Re-Kernel';
            const kernelMajor = process.env.KERNEL_MAJOR;

            core.info(`Fetching the latest release for ${owner}/${repo}...`);

            const release = await github.rest.repos.getLatestRelease({
              owner,
              repo,
            });
            core.info(`Latest version: ${release.data.tag_name}`);
            const escapedKernel = kernelMajor.replace(/\./g, '\\.');
            const pattern = new RegExp(`^Re-Kernel-v[\\d.]+-${escapedKernel}\\.zip$`);

            let targetAsset = null;
            for (const asset of release.data.assets || []) {
              if (pattern.test(asset.name)) {
                targetAsset = asset;
                break;
              }
            }

            if (!targetAsset) {
              core.setFailed(`No matching Re-Kernel module asset found (pattern: ${pattern.source})`);
              return;
            }

            core.info(`Found target asset: ${targetAsset.name} (${(targetAsset.size / 1024).toFixed(1)} KB)`);
            core.setOutput('asset-name', targetAsset.name);
            core.setOutput('download-url', targetAsset.browser_download_url);
            core.setOutput('tag-name', release.data.tag_name);

      - name: 📥 Download Re-Kernel module
        if: ${{ env.ENABLE_REKERNEL == 'true' }}
        working-directory: kernel_workspace
        env:
          ASSET_NAME: ${{ steps.download-rekernel.outputs.asset-name }}
          DOWNLOAD_URL: ${{ steps.download-rekernel.outputs.download-url }}
          ARTIFACT_NAME: Re-Kernel-v${{ steps.download-rekernel.outputs.tag-name }}-6.6
        run: |
          set -euo pipefail
          echo "Downloading Re-Kernel module: $ASSET_NAME"
          echo "Download URL: $DOWNLOAD_URL"
          aria2c -s16 -x16 -k1M "$DOWNLOAD_URL" -o "$ASSET_NAME"
          echo "✓ Re-Kernel module download completed"
          ls -lh "$ASSET_NAME"
          echo "Creating directory $ARTIFACT_NAME and extracting the module..."
          mkdir -p "$ARTIFACT_NAME"
          unzip -q "$ASSET_NAME" -d "$ARTIFACT_NAME"
          echo "✓ Directory structure:"
          ls -lR "$ARTIFACT_NAME"

      - name: 📤 Upload Re-Kernel module
        if: ${{ env.ENABLE_REKERNEL == 'true' }}
        uses: actions/upload-artifact@v5
        with:
          name: Re-Kernel-v${{ steps.download-rekernel.outputs.tag-name }}-6.6
          path: kernel_workspace/Re-Kernel-v${{ steps.download-rekernel.outputs.tag-name }}-6.6
